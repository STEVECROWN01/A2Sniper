fastapi>=0.104.1
uvicorn>=0.24.0
pydantic>=2.5.0
# Required by FastAPI to parse multipart/form-data requests (avatar upload).
# Without it, `await request.form()` raises:
#   "The `python-multipart` library must be installed to use form parsing."
python-multipart>=0.0.6
pandas>=2.2.0
numpy>=1.26.2
scipy>=1.12.0
python-telegram-bot>=20.7
sqlalchemy>=2.0.23
asyncpg>=0.29.0
aiosqlite>=0.19.0
websockets>=12.0
python-dotenv>=1.0.0
httpx>=0.25.2
pocketoptionapi-async>=2.0.1
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.2
# Web Push notifications — sends push notifications to users' devices when
# signals fire on the signals page. Works even when the browser is closed.
pywebpush>=2.0.0
# AI/ML dependencies REMOVED (scikit-learn, xgboost, torch) — the neural_models
# package (LSTM, Transformer, XGBoost voting classifier) was dead code:
# voting_model.predict() was never called, model weights were never trained,
# and PyTorch was not in requirements. The package has been deleted.
# Re-add these if you build a real AI layer:
#   scikit-learn>=1.4.0
#   xgboost>=2.0.2
#   torch>=2.2.0  # ~200MB, slows startup by 30s
