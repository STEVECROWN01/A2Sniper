[phases.setup]
nixPkgs = ['python311']

[phases.install]
cmd = "pip install --no-cache-dir -r backend/requirements.txt"

[phases.build]
cmd = "echo 'Build complete'"

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
